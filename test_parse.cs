using System; using System.IO; using Newtonsoft.Json.Linq; class Program { static void Main() { var renderer = JToken.Parse("{ \"playlistItemData\": { \"videoId\": \"12345\" }, \"flexColumns\": [ { \"musicResponsiveListItemFlexColumnRenderer\": { \"text\": { \"runs\": [ { \"text\": \"Test Title\" } ] } } } ] }"); var track = YTMusicWP.Services.InnerTubeClient.ParseMusicListItem(new JObject { ["musicResponsiveListItemRenderer"] = renderer }); Console.WriteLine(track?.Title + " " + track?.VideoId); } }
